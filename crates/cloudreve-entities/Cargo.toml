[package]
name = "cloudreve-entities"
version = "0.1.0"
edition = "2024"
rust-version = "1.94.0"
publish = false

[dependencies]
sea-orm = { version = "2.0.0-rc.42", default-features = false, features = ["macros", "with-chrono", "with-json", "with-uuid"] }
